{
  "dependencies": {
    "reflect-metadata": "^0.1.12",
    "ts-node": "^7.0.1",
    "typeorm": "^0.2.8",
    "typeorm-model-generator": "^0.2.22"
  },
  "scripts": {
    "start": "tsc",
    "models": "typeorm-model-generator --noConfig -e mysql --cf camel --ce camel --cp camel -d mtr -o entities/",
    "typeorm": "./node_modules/.bin/typeorm",
    "create-migration": "yarn typeorm migration:create",
    "run-migration": "yarn typeorm migration:run"
  }
}
